<% if object.errors.any? %>
  <div class="row">
    <div class="alert-box alert">
      <%= pluralize(object.errors.size, "Errors") %>
      <ul>
        <% object.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>